#include <bits/stdc++.h>

#ifndef INC_COMMAND_MODE_H
#define INC_COMMAND_MODE_H

#include "terminal.h"

void enter_in_command_mode(vector < tuple < string, string, char > > file_list, terminal &app);

#endif